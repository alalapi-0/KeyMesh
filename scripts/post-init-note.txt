初始化完成！建议下一步：
1) 使用 scripts/gen-certs.* 生成 CA 与节点证书材料。
2) 在 config.yaml 的 security 段更新证书路径与节点指纹白名单。
3) 根据你的 VPN/中继环境为 peers[].addr 填写可达地址。
4) 运行 `python -m keymesh check` 验证配置与路径。
5) 目前 run/add-peer 为占位，Round 2 起将逐步实现实际同步逻辑。
